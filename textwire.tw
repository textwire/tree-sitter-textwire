<body>
    <div>
        <h1>Nice one</h1>
        <p>Hey there</p>

        @if(true)
            <span>{{ -1 + 2.23 }}</span>
            <span>{{ nil }}</span>
        @end

        @each(user in users)
            <span>{{ user.name.upper() }}</span>
        @end
    </div>
</body>
